1. 交互策略 (Interaction Strategy)
先思考后行动：在修改复杂逻辑前，先用简单的步骤描述你的思路。

回答偏好：保持回答简洁、专业。除非我要求，否则不需要解释基础语法。

文件意识：在创建新文件前，检查项目中是否已有类似功能的文件，优先进行重构而非重复造轮子。

确认变更：如果你的修改会破坏现有 API 或核心逻辑，请先提醒我。

2. 代码质量标准 (Code Quality Standards)
DRY 原则：不要重复你自己。如果一段逻辑出现三次，提取为可复用的函数或组件。

清晰胜过简洁：变量和函数命名应具备描述性，避免使用 a, b, data 等模糊词汇。

模块化：保持函数单一职责（Single Responsibility）。一个函数只做一件事，且长度尽量控制在 30 行以内。

错误处理：不要忽略错误（不要使用空的 catch 块）。必须包含基础的错误捕获和合理的日志/反馈。

3. 文档与注释 (Documentation)
自解释代码：代码结构和命名应清晰到不需要注释也能看懂。

逻辑注释：仅针对“为什么这么做”而非“这段代码做了什么”编写注释。

接口文档：公共函数和 API 必须包含标准的文档注释（如 JSDoc, Docstring 等）。

4. 格式与提交规范 (Formatting & Git)
保持风格一致：自动检测项目已有的缩进（空格或 Tab）和括号风格，并严格遵守。

清理无用代码：移除调试用的 print/console.log 以及未使用的导入（Unused imports）。

小步快跑：给出的方案应该是增量式的，避免一次性重写整个文件。

5. 性能与安全 (Performance & Security)
安全第一：严禁在代码中硬编码任何 API Key、密码或敏感配置。

资源意识：考虑算法复杂度，避免在循环中进行高能耗操作（如频繁的 I/O 或数据库查询）。
