import { PrestoBrandHeading } from "../brand/PrestoBrandHeading";

export function ShownotesBrandHeading({ className = "" }: { className?: string }) {
  return <PrestoBrandHeading productLabel="Shownotes" className={className} />;
}
