# API Spec

当前项目日志管理相关 OpenAPI 定义见 `docs/api/openapi.yaml`。

覆盖接口：

- `GET/POST /api/admin/log-management`
- `GET /api/admin/log-events`
- `POST /api/frontend-global-error`

后续建议将其它核心业务 API 逐步纳入该规范，最终作为统一接口契约源。
